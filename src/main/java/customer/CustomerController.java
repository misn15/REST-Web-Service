package customer;

import org.springframework.web.bind.annotation.RestController;

// Controller class will handle HTTP requests

@RestController // Rest controller - don't need @ResponseBody because it automatically serializes the return object into HttpResponse
public class CustomerController {
	
}
